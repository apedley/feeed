{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "dist",
  "scripts": {
    "dev": "nodemon -w src --exec \"babel-node src --presets es2015,stage-0\"",
    "debug": "nodemon -w src --exec \"babel-node --debug-brk src --inspect --presets es2015,stage-0\"",
    "build": "babel src -s -D -d dist --presets es2015,stage-0",
    "start": "node dist",
    "prestart": "npm run -s build",
    "test": "eslint src",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "boom": "^6.0.0",
    "glob": "^7.1.2",
    "hapi": "^16.6.2",
    "hapi-auth-jwt": "^4.0.0",
    "joi": "^11.1.1",
    "mongoose": "^4.11.14",
    "mongose": "0.0.2-security",
    "rest-hapi": "^0.28.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "nodemon": "^1.12.1"
  }
}
